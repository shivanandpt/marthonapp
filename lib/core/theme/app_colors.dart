import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0A0A0A);
  static const primary = Color(0xFFFF6B00);
  static const secondary = Color(0xFF00C2FF);
  static const accent = Color(0xFF29FF94);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFAAAAAA);
  static const cardBg = Color(0xFF1C1C1E);
  static const error = Color(0xFFFF4C4C);
  static const disabled = Color(0xFF444444);
  static const progressBg = Color(
    0xFF222222,
  ); // subtle dark bg for progress bars
  static const progressPace = Color(0xFF00C2FF); // Cyan
  static const progressDistance = Color(0xFF29FF94); // Green
  static const progressHeartRate = Color(0xFFFF4C4C); // Red
  static const progressCalories = Color(0xFFFFA500); // Orange
  static const progressDailyGoal = Color(0xFFFFD700); // Yellow
}
